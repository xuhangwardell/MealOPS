# MealOps 项目协作规则

## 适用范围

本文件适用于整个 MealOps 仓库。若未来某个子目录需要更具体的规则，可以在该子目录增加 `AGENTS.md`；子目录规则只能细化当前规则，不能擅自扩大产品范围或绕过质量要求。

## 产品定位

MealOps 是面向独居人群的库存感知型多餐规划系统。

它解决的不是简单推荐“这一顿吃什么”，而是帮助用户在库存、保质期、烹饪时间、预算、饮食偏好、带饭需求和食材复用等约束下，规划未来 1～3 天的多顿餐食，尽量少购买、少浪费、少重复做饭。

核心业务闭环为：

1. 维护标准食材和结构化菜谱；
2. 记录库存批次及保质期；
3. 输入未来 1～3 天的用餐需求；
4. 生成多个餐食计划候选；
5. 聚合计划所需食材；
6. 使用有效库存抵扣需求；
7. 生成缺失食材购物清单；
8. 用户确认做饭结果；
9. 更新库存和用餐计划状态。

MealOps 不是普通菜谱推荐应用，也不是大模型聊天机器人。

## 事实来源与 AI 边界

- 确定性业务系统是库存、菜谱、计划和购物清单的唯一事实来源。
- 规划核心必须由可测试、可复现、可解释的确定性逻辑完成。
- LLM 即使在后续版本引入，也只能通过受控的应用服务或工具读取和操作业务状态。
- LLM 不得直接访问或修改数据库，不得编造食材、菜谱、库存批次或计划标识。
- V1 不引入 LLM、Agent Tool Calling、RAG 或向量数据库。

## 开发策略与架构边界

- 按可独立验收的纵向业务节点逐步实现，一次只完成当前任务范围。
- 不提前创建未来节点的表、接口、模块、占位实现或依赖。
- 采用模块化单体，不使用微服务。
- V1 不引入 Redis、消息队列、RAG、向量数据库或 LLM。
- 后端业务代码按业务域组织；Controller 只负责协议适配、输入校验和响应转换，不得包含业务逻辑。
- 应用服务负责用例编排与事务边界；可独立表达的领域规则应与协议层、持久化层解耦。
- 不创建无明确业务价值的通用基类、抽象层或设计模式。

## 已确认技术基线

- 仓库采用 Monorepo，前后端分离；后端是 package-level modular monolith。
- 后端使用 Java 21、Spring Boot 4.1.x、Spring MVC 和 Maven。
- 后端当前只使用一个 Maven `backend` module，不拆 Maven 多业务模块。
- 数据库使用 PostgreSQL 18，所有 schema 变更使用 Flyway。
- 持久层使用 MyBatis-Plus 3.5.x Spring Boot 4 starter，但仅用于减少简单 CRUD 样板代码。
- 禁止用 MyBatis-Plus `IService`、`ServiceImpl` 或 ActiveRecord 作为业务架构。
- 核心查询、复杂排序、聚合、库存查询和 Planner Candidate 查询必须使用可审查的显式 SQL。
- 业务 Service 和领域接口不得暴露 MyBatis-Plus Wrapper 或其他持久层框架类型。
- API 使用 REST、JSON 和 `/api/v1` 前缀；成功响应使用标准 HTTP status，不统一包装 `Result<T>`。
- 错误响应使用 RFC 9457 Problem Details，并可扩展稳定业务错误码。
- API 文档使用与 Spring Boot 4 兼容的 springdoc-openapi 3.x。
- 测试使用 JUnit 5、AssertJ、Spring Boot Test 和 Testcontainers。
- PostgreSQL、Flyway、Mapper、事务、锁和数据库约束必须使用真实 PostgreSQL 集成测试，不使用 H2 替代。
- 前端使用 uni-app CLI/Vite、Vue 3、TypeScript 和 Pinia；第一目标为 H5，第二目标为微信小程序。
- 前端 HTTP Client 封装 `uni.request`，不引入 Axios；大型 UI 组件库暂不选择。
- 后续认证方向为 Spring Security + Bearer Token/JWT，具体令牌策略必须在认证节点通过 ADR 决定。
- V1 不引入 Redis、消息队列或 LLM；后续 Agent 预计使用 Spring AI 2.x，并且只能通过 Tool 调用 Application Service。
- 精确依赖 patch 版本必须在创建工程时验证后固定，不得使用动态版本或仅凭“兼容”声明跳过构建验证。

## Git 安全规则

- 未经用户明确要求，不得自动执行 `git commit`。
- 未经用户明确要求，不得执行 `git push`。
- 不得执行 `git reset --hard`、`git clean -fd`、`git rebase`，或其他可能导致工作区、暂存区、提交历史内容丢失或被改写的操作。
- 每个纵向业务节点必须先完成实现、测试和审查，然后等待用户确认；只有用户明确要求后才能提交。
- 一个 Git 提交只能包含一个当前纵向业务节点的相关变更，不得混入其他节点、顺手重构或无关格式化。

## 架构决策记录

- 重要技术决策必须在 `docs/decisions` 下通过 ADR 记录，并与对应实现保持一致。
- 需要 ADR 的事项包括但不限于：数据库选择、ORM 或 Mapper 选择、缓存、消息队列、并发控制、幂等方案、Planner 算法、Agent 架构和第三方服务。
- ADR 至少包含以下部分：
  1. Background：问题背景、当前约束和需要决定的事项；
  2. Decision：最终采用的方案及适用边界；
  3. Reasons：选择该方案的具体理由；
  4. Alternatives：评估过但未选择的替代方案；
  5. Trade-offs：当前方案的收益、代价和风险；
  6. Revisit Conditions：未来需要重新评估该决策的触发条件。
- 尚未完成比较或未经用户确认的重要决策必须标记为“待确认”，不得直接通过代码将其固化。

## 第三方依赖规则

- 新增任何第三方依赖前，必须先说明：
  1. 该依赖解决的具体问题；
  2. 为什么现有语言、框架或仓库能力无法合理满足；
  3. 至少一个可行的替代方案；
  4. 为什么选择当前依赖；
  5. 对构建时间、产物体积、运行时行为、安全性、升级和长期维护的影响。
- 禁止因为未来可能使用、示例代码方便或避免少量当前实现工作而提前引入依赖。
- 依赖必须服务于当前已批准纵向节点，并保持最小必要范围；移除依赖时也要检查其配置、调用和文档是否完整清理。

## 数据与数据库规则

- 金额、数量和单位换算使用 `BigDecimal`，不得使用浮点数承载精确业务值。
- 数据库结构变更必须通过 Flyway 迁移完成，不允许依赖手工改表。
- 库存按批次管理；任何库存变化都必须保留不可缺失的库存流水。
- 涉及多条库存记录、库存流水或幂等记录的写操作必须明确事务边界。
- 重要业务不变量应由应用校验与数据库约束共同保护。
- 索引必须对应明确查询场景，不能为“以后可能需要”而提前添加。
- 待确认的数据模型不得自行定为最终方案，应在文档中标记“待确认”。

## API 与代码职责

- Controller 不得直接执行数据库操作或承载业务判断。
- 外部输入必须校验，错误信息应稳定且可解释。
- 持久化实体不得直接作为外部 API 模型返回。
- 业务规则不能复制到 Controller、定时任务、Agent 工具等多个入口。
- 不得为完成当前节点而创建与当前验收无关的未来接口。

## 测试与验收

- 每个纵向节点必须定义可观察的业务验收结果。
- 核心确定性算法优先使用纯单元测试覆盖。
- 数据库约束、事务、并发和幂等行为使用适当的集成测试覆盖。
- 除正常路径外，必须考虑非法输入、边界数量、失败回滚和重复请求。
- 只能报告实际执行过的命令和真实结果；失败、跳过或因环境无法执行的检查必须明确说明。
- 在工程和测试工具尚未建立的节点，不得虚构测试命令或测试通过数量。

## 每次任务的执行流程

1. 阅读本文件、相关文档和现有实现；
2. 确认当前节点目标、明确不做事项和受影响范围；
3. 对非小型任务先给出实施计划；
4. 实现满足验收条件的最小完整纵向切片；
5. 运行与变更风险相匹配的测试和静态检查；
6. 更新受影响的文档；
7. 检查是否出现范围膨胀或文档与实现不一致。

## 每次任务完成后的必需输出

每次任务结束时必须：

1. 总结新增和修改的文件；
2. 说明具体实现内容；
3. 解释关键方案为什么这样选择；
4. 对比没有选择的主要替代方案；
5. 列出实际运行的测试或检查命令；
6. 如实报告每项测试或检查结果；
7. 列出当前限制、遗留问题和所有“待确认”事项；
8. 明确说明是否创建了当前范围之外的文件或功能。

不得因为测试环境缺失而宣称成功，不得在完成当前节点后自动开始下一阶段。

## V1 范围

V1 的完整包含项和排除项以 `docs/product/mvp-scope.md` 为准。任何范围变化必须先更新产品文档和决策记录，再进入实现。
